import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.tum.cit.ase.apollon2',
  appName: 'Apollon Capacitor Wrapped App',
  webDir: 'build/webapp'
};

export default config;
